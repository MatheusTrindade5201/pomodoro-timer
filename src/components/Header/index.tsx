import { HeaderContainer, Logo } from "./style"

const Header = () => {
    return(
        <HeaderContainer>
            <Logo>Pomodoro<p>Timer</p></Logo>
        </HeaderContainer>
    )
}

export default Header